# Google日本語入力相当機能の対応表

| 出典 | 目的 | 具体対象 | 役割 | 前後関係 | 初出定義 | 候補語 |
|---|---|---|---|---|---|---|
| ユーザー依頼「全部実装」 | 相対語だけで暦情報を入力できるようにする | 入力した「きょう」「あした」「ことし」「こんげつ」「いま」「にちじ」などを現在日時に応じた西暦、和暦、曜日、時刻へ変換した候補 | 値 | 読みを入力してSpace変換した後に候補へ追加する | 日時候補とは、入力時点のローカル日時から計算した表示文字列を指す | `SpecialConversionRewriter` |
| ユーザー依頼「全部実装」 | 4桁だけで日付または時刻を入力できるようにする | 入力した4桁の数字を、成立する月日およびMozc準拠の0:00〜29:59の時分として表した候補 | 値 | 数字を入力してSpace変換した後に候補へ追加する | 4桁日時候補とは、4桁を先頭2桁と末尾2桁に分けて成立する月日または翌日早朝を含む業務時刻表記を指す | `SpecialConversionRewriter` |
| ユーザー依頼「全部実装」 | IME内で四則計算を完結させる | 末尾が半角または全角の等号で終わる数式を評価した有限な数値候補 | 値 | 数式と等号を入力してSpace変換した後に候補へ追加する | 計算結果候補とは、許可した演算子と括弧だけからなる式の評価結果を指す | `SpecialConversionRewriter` |
| ユーザー依頼「全部実装」 | Unicodeコードポイントから文字を入力できるようにする | `U+` に続く有効なUnicodeスカラー値を対応する1文字へ変換した候補 | 値 | コードポイントを入力してSpace変換した後に候補へ追加する | Unicode文字候補とは、有効なUnicodeスカラー値が表す文字を指す | `SpecialConversionRewriter` |
| ユーザー依頼「全部実装」 | 実行中のKarukanを識別できるようにする | 「ばーじょん」の変換候補として表示するKarukanのパッケージバージョン | 値 | 読みを入力してSpace変換した後に候補へ追加する | バージョン候補とは、ビルドされたKarukanのパッケージバージョン文字列を指す | `SpecialConversionRewriter` |
| ユーザー依頼「全部実装」 | ローマ字入力だけで頻用記号を直接入力する | `zh/zj/zk/zl/z[/z]/z,/z./z//z-` に対応する矢印、括弧、約物 | 値 | ローマ字列を入力した時点でかな変換規則が文字へ置換する | Zコマンドとは、`z` と1文字の組み合わせを頻用記号へ直接置換する規則を指す | Zコマンド |

| 出典 | 目的 | 具体対象 | 役割 | 前後関係 | 初出定義 | 候補語 |
|---|---|---|---|---|---|---|
| ユーザー指示「郵便番号一旦スルー」 | 今回の変更範囲を郵便番号以外へ限定する | 郵便番号から住所への変換は実装・データ追加・検証を行わず保留する | 状態 | 全機能の調査後、実装開始前に今回の対象外へ変更された | 郵便番号保留とは、今回の完了条件から郵便番号住所変換を除外した状態を指す | 郵便番号保留 |
| ユーザー依頼「全部実装」 | 日本語読みから英単語を入力できるようにする | カタカナ語またはひらがな語の読みに対応する英字表記の候補 | 値 | 読みを入力してSpace変換した後に辞書候補として表示する | 英単語候補とは、辞書に記録された日本語読みに対応する英字表記を指す | 英単語候補 |
| ユーザー依頼「全部実装」 | よくある誤入力から正しい語を選べるようにする | 登録済みの誤読または誤綴りに対応する訂正語の候補 | 値 | 誤入力をSpace変換した後に辞書候補として表示する | 訂正候補とは、辞書に明示された誤入力に対応する正しい表記を指す | 訂正候補 |
| ユーザー依頼「全部実装」 | 読みから顔文字・絵文字・記号を選べるようにする | 「かおもじ」「にこにこ」「はーと」「きごう」などに対応する文字列候補 | 値 | 読みを入力してSpace変換した後に候補へ追加する | 表現候補とは、既存の絵文字・記号変換表と追加する顔文字表から得る候補を指す | 表現候補 |
| 既存Karukan実装 | 数字から複数表記を選べる状態を維持する | 半角・全角、漢数字、大字、ローマ数字、丸数字、2進数、8進数、16進数の既存候補 | 値 | 数字を入力してSpace変換した後に既存の数値変換が候補へ追加する | 数値表現候補とは、既存の `NumberRewriter` が生成する数字の表記違いを指す | `NumberRewriter` |

| 出典 | 目的 | 具体対象 | 役割 | 前後関係 | 初出定義 | 候補語 |
|---|---|---|---|---|---|---|
| 独立利用者監査 | 特殊候補を最初の候補ページで選べるようにする | 学習候補とユーザー辞書候補の後、モデル・システム辞書・通常表記候補の前へ特殊候補を挿入する順序 | 手段 | Space変換で全候補を組み立てる時に一度だけ適用する | 特殊候補優先順とは、明示的な特殊入力の結果を初頁へ置きつつ学習とユーザー辞書を尊重する順序を指す | 特殊候補優先順 |
| 最終利用者監査 | 動的な日時や計算結果を翌回以降の固定候補として残さない | 特殊変換が生成した候補を通常の表記変換と区別し、確定時の学習対象から除外する候補出自 | 状態 | 特殊候補を候補列へ追加してから確定処理が学習可否を判定するまで保持する | 特殊変換候補出自とは、入力時点で再計算すべき値であり学習キャッシュへ保存しない候補の出自を指す | `CandidateSource::SpecialConversion` |
| 最終利用者監査 | 通常開始と変換範囲変更のどちらでも特殊候補を学習対象へ戻さない | 出自付きの内部候補列を、特殊候補だけ読みを持たない公開候補列へ変換する一つの処理 | 手段 | 候補の優先順を組み立てた後、変換状態へ入る前にすべての候補再構築経路から呼ぶ | 出自保持候補変換とは、候補の生成元に従って確定時の学習可否を公開候補の読みに写す処理を指す | `candidate_list_from_annotated` |
| 独立利用者監査 | `U+611B`を通常のSpace変換で選べるようにする | 英字直接入力中でも、入力全体が有効な特殊入力ならSpaceを空白挿入ではなく候補変換として扱う条件 | 条件 | 英字直接入力中にSpaceを押した時、特殊候補の有無を判定してから空白挿入へ進む | 特殊入力変換条件とは、英字直接入力中の文字列から特殊候補が生成できることを指す | 特殊入力変換条件 |
| 日時変換の単体試験 | 実時間と固定時間で同じ変換計算を使う | 年月日・時分秒・曜日を一組で保持し、相対日付と表示形式の計算へ渡す値 | 記録 | 実行時はOSのローカル時刻から作り、単体試験では既知の値から作る | ローカル日時とは、Karukanを実行しているOSの時間帯における暦日と時計の成分を指す | `LocalDateTime` |
| 日時変換の単体試験 | 時計の現在値に依存せず境界日を検証する | 入力文字列と指定したローカル日時から候補を生成する処理 | 手段 | 実行時の現在時刻取得後と固定値を使う試験の双方から呼ぶ | 指定日時変換とは、与えられたローカル日時を基準に特殊候補を計算する処理を指す | `rewrite_with_local_datetime` |
| Google日本語入力の元号切替年 | 西暦の年から切替前後の和暦候補を欠かさず出す | 元号開始年と一致する西暦では新元号元年と旧元号の最終年を順序付きで返す処理 | 手段 | 西暦年の特殊変換と現在年の候補生成時に適用し、月が既知ならその月に有効な元号へ絞る | 元号候補生成とは、西暦年と任意の月から有効な一つ以上の和暦表記を求める処理を指す | `era_candidates` |
| 顔文字変換データ | 読み別検索と「かおもじ」の全件表示を一度の解析結果から行う | 読みから顔文字列への索引と、上流表順を維持した顔文字列の組 | 記録 | 初回の顔文字変換時にTSVから構築し、以後の変換で再利用する | 顔文字索引とは、読み別候補と全候補の両方を保持する解析済みデータを指す | `EmoticonIndex` |
| ユーザーが列挙した計算例 | 許可した数式だけを安全に評価する | 数字、小数点、括弧、加減乗除、累乗を読み進める位置と入力列を保持する解析状態 | 状態 | 末尾の等号を除去した後、標準的な演算子優先順位で全入力を消費する | 数式解析状態とは、評価対象の文字列と現在の読み取り位置を指す | `ExpressionParser` |
